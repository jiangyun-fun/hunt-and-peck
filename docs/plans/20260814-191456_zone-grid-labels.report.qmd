---
title: "Group view v2 (fixed 5×5 zone grid, all-letter labels) — completion report"
author: "Yun Jiang"
date: 2026-08-14
format:
  html:
    html-math-method: mathjax
include-in-header:
  text: |
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🔤</text></svg>">
---

# What was done

Replaced the group view's derived (irregular) boxes with a **fixed 5×5 zone
grid** and made labels **all letters** (user-confirmed 2026-08-14: 5×5, all 25
letters A–Z minus Q, letters-only second level; `;'`,/` removed from
`HintCharacters`).

- Level 1: 25 regular dotted boxes over the session bounds, keyed A–Z minus Q
  in scan order (`GroupZones="5x5"`, hot-reload).
- Grid-session labels are now **zone-based**: first char = the zone key,
  second char cycles the char set within the zone; a single-point zone is
  labeled by its key alone and fires on one keystroke. Zone-based in BOTH
  views, so `<leader>p` never relabels mid-session.
- Grid cap becomes zones×chars = 25×25 = 625 (was 841) — the grid is ~16%
  coarser by construction so zones fit the second-char budget.
- Safety valve: a zone denser than the char set (edge-dense layouts), or an
  invalid/oversized spec, falls back to the v1 scan-order labels + derived
  boxes shipped earlier today.
- `; ' , /` are no longer configured label chars → they pass through to the
  app while the overlay is up (the hook captures punctuation only when
  configured).

# Verification status (evidence)

| Item | Evidence |
|------|----------|
| Unit tests | CI run `31793000138` step `Test` ✓ (incl. 7 new GroupViewService tests: spec parse, cap, assignment, boundary determinism, overflow) |
| Build | same run, `Build (Release, Any CPU)` ✓ |
| Shipped config | grep of the downloaded artifact: `HintCharacters=ABCDEFGHIJKLMNOPRSTUVWXYZ`, `GroupZones=5x5`, `GroupViewEnabled=true` |
| Drop on the box | ssh `ls`: `$HAP_WIN_TEST_DIR/hap-zonegrid/hap.exe`, 570,368 bytes |

**Not yet verified (needs the Windows box, manual):** the on-box matrix — 25
regular boxes render; zone char narrows to letter second chars; 2nd char
fires; single-point zone fires on one keystroke; punctuation passes through;
`<leader>p` keeps labels stable; Esc ladder; monitor cycle; Automation
unchanged.

# Fix round 1 (2026-08-14, after on-box test)

Symptom: "it's still one row with multiple cols" — the first build silently
fell back to the v1 tall strips. Root causes and fixes in `6a9cf41`:

1. **Zone overflow → silent fallback**: the cap loop accepted 627 points
   whose zones held up to 28 points vs the 25-char budget (spanEdges
   rounding + ceil-split; reproduced by arithmetic). Fix: the loop now
   coarsens until the largest zone fits (`MaxZoneCount`); converges to
   ~29×16 = 464 points, max zone 24 on 1080p (simulation of the exact
   generation math).
2. **Absolute-vs-relative coordinates**: zone lookups and boxes used the
   session bounds' absolute origin, but PointHint rects and the canvas are
   session-relative — broken on any monitor not at (0,0). Fix: bounds is a
   pure size; lookups and boxes are relative.

CI `31794460021` caught a wrong expectation in the new regression test
(two points in different single-point zones are each 1-char labeled);
assertion fixed in `c2f80ff`; `31794575390` green. Current drop:
`$HAP_WIN_TEST_DIR/hap-zonefix/hap.exe`.

# Fix round 2 (2026-08-14)

On-box: quadrant hotkeys (Ctrl+Shift+F1..F4) still showed v1 strips, and the
key char moved to the box center. Quadrant sessions set
`OwningWindowBounds = monitor` (full-screen overlay) while their points
cluster in one quarter, so bounds-based zone slicing overflowed. Fix
(`5c2249d`): zones tile the hints' extent (`HintExtent`) in both
`TryAssignZoneLabels` and the provider cap loop's `MaxZoneCount`; key-char
pill centered. CI `31796151578` green; drop `hap-zonectr`.

# Fix round 3 (2026-08-14)

User asks: uniform 6×4 second-level layout; leader should not reset the typed
prefix. Both agreed with evidence: the 6×3/6×4 mix is the uneven 29×16-into-
25 split; the leader clear's rationale is void (dispatch branches on the
pending flag). Fix (`88dfa69`): zone-ALIGNED generation (every zone exactly
6×4=24 on 16:9, 30×20=600 total, per-zone uniformity verified by simulation
{24}); `<Space>` preserves the prefix (snapshot/select still clear on entry).
`MaxZoneCount`/`EffectiveGridCap` removed as superseded. CI `31799957316`
green; drop `hap-zone64`.

# Commits

- `4588d07` feat(overlay): zone-grid group view (default 5x5) + all-letter labels
- `6a9cf41` fix(overlay): zone-grid fell back to v1 strips (zone overflow + abs coords)
- `c2f80ff` test(overlay): fix relative-coords regression test expectation

Plan: `20260814-183116_zone-grid-labels.plan.qmd` (rewritten in place to match
what was built).
